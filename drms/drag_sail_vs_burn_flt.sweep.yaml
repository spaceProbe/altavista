# ParameterSweep (proto/altavista/v1/system.proto), YAML transcoding -- F4b
# (docs/feasibility-plan.md's F4 milestone, second grid). Emitted by altavista.feasibility
# (NOT hand-written -- altavista/feasibility/study_drag_sail_vs_burn.py's own
# build_grid_flt()/emit_sweep_yaml() call wrote this file and computed `hash:` below through
# the package, which shells out to the same `cargo run -p av-sweep --example sweep_hash` the F4
# grid's own sweep.yaml header names). This header comment is a pure text prepend added after
# emission (altavista/feasibility/yaml_io.to_yaml has no header-comment field to author
# through -- the same disclosed gap drms/drag_sail_vs_burn.sweep.yaml's own header already
# names, still not silently patched around here). A YAML comment is never part of the parsed
# message, so prepending it does not change `hash:` below -- reverified with the same
# `cargo run -p av-sweep --example sweep_hash` command after this edit (see
# docs/studies/drag-sail-vs-burn.md's own "Hashes and reproduction" section for both the before-
# and after-prepend digests).
#
# **F4b also changed the emitter itself** (altavista/feasibility/yaml_io.py): it now omits a
# field when it equals its own proto3/Rust zero default, instead of always writing every field
# explicitly -- which is why this file is far more compact than
# drms/drag_sail_vs_burn.sweep.yaml's own fully-explicit style (that file was emitted by the
# PRE-F4b version of the emitter and was intentionally left untouched, not re-emitted, so its
# own committed hash stays exactly what this task's brief already commits to). The change is
# hash-neutral by construction (an omitted field and an explicit zero-default value parse to the
# identical Rust value under `#[serde(default)]`) -- proved, not just argued, by re-emitting
# drms/drag_sail_vs_burn.sweep.yaml's own declaration through the new emitter and confirming its
# computed hash still matches `a67a6bf1be781bda13b9d0c204582cc849ef58d6abeb4b6e3ce26b6bd7689fee`
# (docs/studies/drag-sail-vs-burn.md's own emitter-change section has the full command and
# digest).
#
# The study (second grid, F4b): 2 (demo_flt.spacecraft.DragArea: 5.0, 25.0 m^2) x 3 (event
# burn1's dv_x: 10.0, 20.0, 30.0 m/s) = 6 points x 3 monte_carlo_draws = 18 samples, against the
# SAME, unmodified drms/drag_sail_vs_burn.drm.yaml the first grid (id: drag_sail_vs_burn) also
# runs against -- see that DRM's own header comment for the scenario, the Gates sigmas, and the
# first grid's own measured finding that a DragArea axis on demo_mvr is physically inert there.
#
# This grid moves the DragArea axis to demo_flt -- the instance that genuinely carries a
# DragForce (JacchiaRoberts + the CSSI weather-source trio,
# drms/demo_two_instance.sos.yaml's own parameter_overrides on demo_flt) -- while leaving the
# dv_x axis unchanged on demo_mvr's own burn1. DragArea axis values are IDENTICAL to the first
# grid's own (5.0 = leo_demo_sys's own declared baseline, 25.0 = a deployed drag sail, 5x
# increase -- also the same range demo_two_instance_sweep.sweep.yaml already uses on this same
# instance, demo_flt), chosen for direct apples-to-apples comparability against both the first
# grid's own null result and demo_two_instance_sweep.sweep.yaml's own prior measurement, not to
# make the figure look better. dv_x axis values are unchanged from the first grid, for the same
# reason: an identical draw count and axis structure so the two grids are directly comparable.
#
# `dispersed: true` and `monte_carlo_draws: 3`, matching the first grid exactly.
#
# HYPOTHESIS (written before running; docs/studies/drag-sail-vs-burn.md records the measured
# values against this, and altavista/feasibility/study_drag_sail_vs_burn.py's own module
# docstring has the full writeup for all four axis-to-score sensitivities this grid measures):
# - demo_flt_rmag_at_end vs. DragArea (direct): a real, monotonic, tens-of-metres effect (larger
#   DragArea -> faster decay -> smaller final rmag), based on
#   crates/av-sweep/tests/fixture_study.rs's own prior measurement on this instance/axis pair
#   (~97-101 m) and this task's own manager brief (~47 m).
# - demo_flt_rmag_at_end vs. dv_x (indirect, via the controller's range-latch timing): a real,
#   nonzero coupling exists (dv_x changes demo_mvr's own trajectory, which changes WHEN its rmag
#   crosses demo_ctrl's threshold, which changes how much of the run demo_flt spends at the
#   post-latch Cd=220 vs. the pre-latch Cd=2.2) -- magnitude not tightly predicted in advance.
# - demo_mvr_rmag_at_end vs. dv_x (direct): expected to closely reproduce the first grid's own
#   measured ~2.6 km/(m/s), ~52 km across the full 20 m/s bracket, since demo_mvr's own dynamics
#   do not depend on demo_flt's DragArea (one-directional port wiring: demo_mvr -> demo_ctrl ->
#   demo_flt).
# - demo_mvr_rmag_at_end vs. DragArea (on demo_flt, a different instance): hypothesized EXACTLY
#   ZERO -- a stronger, structural null than the first grid's own (there, DragArea was declared
#   on the scored instance itself but unread; here, DragArea is not even declared on the scored
#   instance, and the port wiring is strictly one-directional away from demo_mvr).
id: drag_sail_vs_burn_flt
drm_id: drag_sail_vs_burn_drm
axes:
- instance: demo_flt
  parameter: spacecraft.DragArea
  values:
  - 5.0
  - 25.0
- values:
  - 10.0
  - 20.0
  - 30.0
  event_id: burn1
  value_key: dv_x
monte_carlo_draws: 3
provenance:
  author_kind: AUTHOR_KIND_AGENT
  tool: F4b drag-sail-vs-burn (flt) study authoring
hash: 2f156ae1063e57f90cd92edd77c60cf509b0a574347cf526474959a8ffcb5b2d
dispersed: true
